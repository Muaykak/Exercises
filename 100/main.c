#include <stdio.h>

int main(){
	printf("Write a C program to convert a currency value (floating point with two decimal places) to the number of coins and notes.\n\n");
	printf("Input the currency value (floating point with two decimal places) :\n");
	double input;
	scanf("%lf", &input);
}
